# Production backend URLs for the web app (Vite reads this during `vite build`).
# Public endpoints only — NO secrets. account-api is deployed (login works);
# the mesh/indexer/nft services still need deploying for map/wallet features.
VITE_ACCOUNT_URL=https://step-account.moldovancsaba.workers.dev
