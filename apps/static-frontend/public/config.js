// Runtime config for static hosting. Override these values after deployment
// without rebuilding, or edit them from the in-app Settings panel.
window.STEP_CONFIG = {
  gatewayUrl: "/api/gateway",
  indexerUrl: "/api/indexer",
  explorerUrl: "/explorer",
  minerUrl: "/miner",
};
